@model List<DataAccessLayer.Entities.InventoryAllocation>
@{
    ViewData["Title"] = "Quản lý tồn kho";
    Layout = "_DashboardLayout";
    var summary = ViewBag.Summary as Dictionary<string, int>;
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h2 class="mb-1">Quản lý tồn kho</h2>
        <p class="text-muted mb-0">Quản lý phân bổ và điều phối tồn kho cho các đại lý</p>
    </div>
    <div>
        <a asp-action="CreateAllocation" class="btn btn-primary">
            <i class="fas fa-plus me-2"></i>Thêm phân bổ
        </a>
    </div>
</div>

<!-- Summary Cards -->
<div class="row g-3 mb-4">
    <div class="col-12 col-md-6 col-lg-3">
        <div class="card border-0 shadow-sm h-100">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <p class="text-muted mb-1 small">Tổng phân bổ</p>
                        <h3 class="mb-0">@(summary?["TotalAllocations"] ?? 0)</h3>
                    </div>
                    <div class="fs-1 text-primary">📦</div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-12 col-md-6 col-lg-3">
        <div class="card border-0 shadow-sm h-100">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <p class="text-muted mb-1 small">Tổng tồn kho</p>
                        <h3 class="mb-0">@(summary?["TotalStock"] ?? 0)</h3>
                    </div>
                    <div class="fs-1 text-success">🚗</div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-12 col-md-6 col-lg-3">
        <div class="card border-0 shadow-sm h-100">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <p class="text-muted mb-1 small">Tồn kho thấp</p>
                        <h3 class="mb-0 text-warning">@(summary?["LowStock"] ?? 0)</h3>
                    </div>
                    <div class="fs-1 text-warning">⚠️</div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-12 col-md-6 col-lg-3">
        <div class="card border-0 shadow-sm h-100">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <p class="text-muted mb-1 small">Hết hàng</p>
                        <h3 class="mb-0 text-danger">@(summary?["OutOfStock"] ?? 0)</h3>
                    </div>
                    <div class="fs-1 text-danger">🚫</div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Quick Actions -->
<div class="row g-3 mb-4">
    <div class="col-12 col-md-6 col-lg-3">
        <div class="card border-0 shadow-sm h-100">
            <div class="card-body text-center">
                <i class="fas fa-list fa-2x text-primary mb-3"></i>
                <h6 class="mb-2">Xem tất cả phân bổ</h6>
                <a asp-action="Allocations" class="btn btn-outline-primary btn-sm">Xem chi tiết</a>
            </div>
        </div>
    </div>
    <div class="col-12 col-md-6 col-lg-3">
        <div class="card border-0 shadow-sm h-100">
            <div class="card-body text-center">
                <i class="fas fa-exclamation-triangle fa-2x text-warning mb-3"></i>
                <h6 class="mb-2">Cảnh báo tồn kho</h6>
                <a asp-action="StockAlerts" class="btn btn-outline-warning btn-sm">Xem cảnh báo</a>
            </div>
        </div>
    </div>
    <div class="col-12 col-md-6 col-lg-3">
        <div class="card border-0 shadow-sm h-100">
            <div class="card-body text-center">
                <i class="fas fa-exchange-alt fa-2x text-info mb-3"></i>
                <h6 class="mb-2">Chuyển kho</h6>
                <a asp-action="StockTransfer" class="btn btn-outline-info btn-sm">Chuyển kho</a>
            </div>
        </div>
    </div>
    <div class="col-12 col-md-6 col-lg-3">
        <div class="card border-0 shadow-sm h-100">
            <div class="card-body text-center">
                <i class="fas fa-history fa-2x text-secondary mb-3"></i>
                <h6 class="mb-2">Lịch sử giao dịch</h6>
                <a asp-action="Transactions" class="btn btn-outline-secondary btn-sm">Xem lịch sử</a>
            </div>
        </div>
    </div>
</div>

<!-- Recent Allocations -->
<div class="card border-0 shadow-sm">
    <div class="card-header bg-white">
        <div class="d-flex justify-content-between align-items-center">
            <h5 class="mb-0">Phân bổ tồn kho gần đây</h5>
            <a asp-action="Allocations" class="btn btn-outline-primary btn-sm">Xem tất cả</a>
        </div>
    </div>
    <div class="card-body">
        @if (Model != null && Model.Any())
        {
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Sản phẩm</th>
                            <th>Đại lý</th>
                            <th>Phân bổ</th>
                            <th>Có sẵn</th>
                            <th>Tối thiểu</th>
                            <th>Trạng thái</th>
                            <th>Cập nhật cuối</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var allocation in Model.Take(10))
                        {
                            <tr>
                                <td>
                                    <div>
                                        <strong>@allocation.Product?.Name</strong>
                                        <br><small class="text-muted">@allocation.Product?.Brand?.Name</small>
                                    </div>
                                </td>
                                <td>
                                    <div>
                                        <strong>@allocation.Dealer?.Name</strong>
                                        <br><small class="text-muted">@allocation.Dealer?.DealerCode</small>
                                    </div>
                                </td>
                                <td><span class="badge bg-primary">@allocation.AllocatedQuantity</span></td>
                                <td>
                                    @{
                                        var availableClass = allocation.AvailableQuantity <= 0 ? "text-danger" : 
                                                           allocation.AvailableQuantity <= allocation.MinimumStock ? "text-warning" : "text-success";
                                    }
                                    <span class="@availableClass fw-bold">@allocation.AvailableQuantity</span>
                                </td>
                                <td>@allocation.MinimumStock</td>
                                <td>
                                    @{
                                        var statusClass = allocation.Status switch
                                        {
                                            "Active" => "bg-success",
                                            "Suspended" => "bg-warning",
                                            "OutOfStock" => "bg-danger",
                                            _ => "bg-secondary"
                                        };
                                    }
                                    <span class="badge @statusClass">@allocation.Status</span>
                                </td>
                                <td>
                                    <small class="text-muted">@allocation.UpdatedAt.ToString("dd/MM/yyyy HH:mm")</small>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="text-center py-4">
                <i class="fas fa-box-open fa-3x text-muted mb-3"></i>
                <p class="text-muted">Chưa có phân bổ tồn kho nào</p>
                <a asp-action="CreateAllocation" class="btn btn-primary">Tạo phân bổ đầu tiên</a>
            </div>
        }
    </div>
</div>
